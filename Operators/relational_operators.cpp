#include<iostream>
using namespace std;
int main()
{
//Relational Operators-These operators are used for the comparison of the values of two operands.
    int a=7,b=4;
    if(a>b)
    {
        cout<<"a is greater than b"<<endl;
    }
    if(a<b)
    {
        cout<<"a is smaller than b"<<endl;
    }
    if(a==b)
    {
        cout<<"a is equals to b"<<endl;
    }
    if(a>=b)
    {
        cout<<"a is greater than equals to b"<<endl;
    }
    if(a<=b)
    {
        cout<<"a is smaller than equals to b"<<endl;
    }
    if(a!=b)
    {
        cout<<"a is not equals to b"<<endl;
    }

}
